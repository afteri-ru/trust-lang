Проанализируй все файлы MEMEORY.md проекта по следующему сценарию:
- Запомни обрщий размер файла.
- Проанализируй его содержание на предмет лишней инфомрации (в нем недолжно быть перечисления очевидных фактой, списков файлов или интерфейсов классов. т..е. в нем недолжно быть любой очевидной инфомарции которую легко получить.
Оставь файле только минимальное описание архитекутры кпоненета, достаточное понимания его работы и важные неочевидные связи, факты и детали, ктоорые сложно получить из общедоступной инфомрации, различные откровения, важные, но неочевидные факты и сложные взаимосвязи и информация о частых ошибках при работе с компонентом
- Переформатируй текст файла в соотвествии с описанными правилами, измерь получившися размер файла и сравни его с изначальным.
- Если новый размер файла стал менее 40% от изначально, уменьши лимит размера такого файла на 20%, если новый размер файла остался более 60% от изначально, то  увелич лимит размера файла на 20%

Проанализируй все файлы MEMEORY.md проекта по этому сценарию, если их размер превышает 4000 байт.
Для файлов размером <= 4000 байт пересчёт лимита не выполняется (лимит остается без изменений). Нижний предел лимита 4000 байт.
Если в каком либо компоненте проекта нет  файла MEMEORY.md (в каталоге include/<name>) создай пустой файл с лимитом 4000 байт.
Размеры лимитов для файлов всегда округляй до 100 в большую сторону.