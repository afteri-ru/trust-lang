# STAT.md - статистика и метрики продуктивности

Критерии анализа итоговых логов задач (`.tasklog/*.md`), метрики продуктивности и методика мониторинга трендов.

---

## 1. Классификация задач

| Статус | Определение | Автоматический признак |
|--------|------------|----------------------|
| `completed` | Есть TaskComplete, повторных открытий нет | 1 запись TaskComplete |
| `reopened` | TaskComplete было ≥2 (задача переоткрывалась после "завершения") | ≥2 записи TaskComplete |
| `interrupted` | Нет TaskComplete, есть обсуждение плана | нет TaskComplete, есть plan_mode_respond |
| `not_started` | Только UserPromptSubmit, обсуждения нет | 0 plan_mode_respond, нет TaskComplete |

---

## 2. Причины доработок (для статуса `reopened`)

Причины извлекаются из переписки между первым и последним TaskComplete.

| Причина | Признак в логе |
|---------|---------------|
| Не проверены тесты/сборка | UserRespond после TaskComplete: "тесты", "сборка", "не компилируется" |
| Не понята семантика типа | UserRespond: "Range это тоже строка", "неправильное поведение" |
| Неполный рефакторинг (забыты связанные файлы) | UserRespond: ссылка на конкретный файл/символ |
| Расширение scope | UserRespond: "добавь ещё...", "а как насчёт..." |
| Не прочитаны связанные структуры | UserRespond: "А их аргументы?", "А это поле?" |

---

## 3. Причины прерывания (для статуса `interrupted` / `not_started`)

Причины извлекаются из **последних 3–5 реплик** планирования в логе.

| Паттерн последнего UserRespond | Вывод о причине |
|---|---|
| Пользователь дал уточнение/вопрос, ответа агента нет | Обсуждение оборвано (ручное прерывание / переключение) |
| "что если...", "а можно..." - агент отвечает, пользователь снова уточняет | Архитектурный поиск без консенсуса |
| Пользователь указал на неверное понимание задачи агентом | Недопонимание требований |
| Последнее сообщение - от агента (plan_mode_respond), ответа нет | Агент ожидает одобрения, пользователь не вернулся |
| Дубликат формулировки другой задачи | Дублирование |
| Только UserPromptSubmit, нет реакции агента | Задача вброшена, не начата |

### Анализ причин брошенных задач (interrupted / not_started)

При разборе `not_started` / `interrupted` фиксировать **приоритетную причину** по категориям:

| Категория | Критерий / пример |
|-----------|-------------------|
| Компонент | Задача относится к компоненту (ast, parser, diag, types, runtime и др.); повторяемость по компоненту — сигнал к ревизии MEMORY.md/README компонента |
| Объём | Задача крупная / недекомпозированная, не разбита на подзадачи, трудоёмкость не оценена |
| Расхождение / непонимание | Неверное понимание требований, несовпадение ожиданий; требуется уточнение постановки |
| Приоритеты / блокер | Задача отложена: в процессе планирования вылезла **блокирующая проблема**, которая стала отдельной следующей задачей; после её решения исходная (отложенная) задача была выполнена |

Причина проставляется в логе задачи (`<id>.md`), чтобы при еженедельном мониторинге трендов можно было считать распределение брошенных задач по категориям и компонентам.


---

## 4. Метрики и формулы

| Метрика | Формула | Ориентир | Текущее (04.08.2026) |
|---------|---------|----------|---------------------|
| `reopen_rate` | reopened / completed | <20% | 47% |
| `interruption_rate` | (interrupted + not_started) / всего | <15% | 22% |
| `plan_adoption` | с планом / всего задач | >85% | 77% |
| `plan_iterations` | медиана plan_mode_respond на задачу | 3 | 3 |
| `tool_calls_per_task` | tool_calls из "## Completion report" (завершённые) или строк в `.tools.log` (незавершённые) | - | - |
| `tool_failure_rate` | failed вызовов / всего вызовов | - | - |
| `resumes_per_task` | число `TaskResume prevMessages=N` | - | - |
| `prompts_per_task` | число `UserPromptSubmit` | - | - |
| `premature_completion` | доля задач с Δ>0 между 1-м и 2-м TaskComplete | - | - |
| `scope_creep_rate` | reopened по причине «расширение scope» / completed | - | - |
| `plan_overrun_rate` | доля задач с plan_iterations>5 (порог из TASK.md) | - | - |
| `median_tool_duration_ms` | медиана duration_ms из `.tools.log` (для всех задач) | - | - |

---

## 5. Мониторинг трендов

- Замер **раз в неделю** по накопленным логам.
- Сравнение с предыдущей неделей по всем метрикам из раздела 4.
- **Фиксация изменений TASK.md** и их влияния на метрики:

| Дата изменения TASK.md | Суть изменения | reopen_rate до | reopen_rate после | Δ |
|---|---|---|---|---|
| 2026-08-04 | Добавлен раздел "Component knowledge check" | 47% | ? | ? |

### Базовый замер (архивный батч, замер 2026-08-24)

Батч архива: **137** завершённых задач (71 `completed`, 66 `reopened`), завершены **2026-06-20 .. 2026-07-21** (перенесены в `.tasklog/archive/archive-2026-06-20_2026-07-21.md`). Отправная точка для сравнения последующих еженедельных замеров по метрикам раздела 4.

| Метрика | Базовый замер (2026-08-24) |
|---------|---------------------------|
| `reopen_rate` | 93.0% |
| `interruption_rate` | 0% (в батче только завершённые) |
| `plan_adoption` | 97.1% |
| `plan_iterations` (медиана) | 4 |
| `tool_calls_per_task` | н.д. |
| `tool_failure_rate` | н.д. |
| `resumes_per_task` | 1.39 |
| `prompts_per_task` | 3.41 |
| `premature_completion` | 98.5% |
| `scope_creep_rate` | 0 |
| `plan_overrun_rate` | 32.1% |
| `median_tool_duration_ms` | н.д. |

### Замер 2026-09-16 (батч архива 2026-07-26 .. 2026-08-05)

Батч архива: **107** завершённых задач (56 `completed`, 51 `reopened`), завершены **2026-07-26 .. 2026-08-05** (перенесены в `.tasklog/archive/archive-2026-07-26_2026-08-05.md`). Сравнение с базовым замером:

| Метрика | Базовый замер (2026-08-24) | Замер 2026-09-16 (батч) | Δ |
|---------|---------------------------|-------------------------|---|
| `reopen_rate` | 93.0% | 91.1% | -1.9 п.п. |
| `interruption_rate` | 0% (только завершённые) | 0% (только завершённые) | 0 |
| `plan_adoption` | 97.1% | 92.5% | -4.6 п.п. |
| `plan_iterations` (медиана) | 4 | 3 | -1 |
| `tool_calls_per_task` | н.д. | н.д. (Completion report у 9/107) | - |
| `tool_failure_rate` | н.д. | н.д. | - |
| `resumes_per_task` | 1.39 | 2.33 | +0.94 |
| `prompts_per_task` | 3.41 | 3.75 | +0.34 |
| `premature_completion` | 98.5% | 91.1% | -7.4 п.п. |
| `scope_creep_rate` | 0 | н.д. (причины доработок не размечены) | - |
| `plan_overrun_rate` | 32.1% | 8.4% | -23.7 п.п. |
| `median_tool_duration_ms` | н.д. | н.д. | - |

### Замер 2026-09-16 (текущий корень `.tasklog/`)

Состав корня: всего **662** задачи: `completed`=471, `reopened`=1, `interrupted`=28, `not_started`=162.

| Метрика | Значение |
|---------|----------|
| `reopen_rate` | 0.2% |
| `interruption_rate` (по формальному признаку) | 28.7% |
| `plan_adoption` | 0% (не измеряется - см. примечание) |
| `plan_iterations` (медиана) | 0 (не измеряется) |
| `prompts_per_task` | 1.13 |
| `resumes_per_task` | 0.01 |
| `premature_completion` | 0.2% |
| `plan_overrun_rate` | 0% |
| `tool_calls_per_task` | 38.2 (среднее по 371 задачам с `## Completion report`) |
| `tool_failure_rate` | 0% |

**Примечание к методике:** маркер `plan_mode_respond` перестал попадать в логи после **2026-08-05** - новые задачи стартуют сразу в ACT и планирования через `plan_mode_respond` не делают. Поэтому для задач, завершённых после 2026-08-05, `plan_adoption`/`plan_iterations` **не измеряются** этим признаком, а классификация `interrupted`/`not_started` по нему **ненадёжна** (ACT-задачи без формального TaskComplete выглядят как `not_started`). Альтернативный признак незавершённости - непустой `.tools.log`: из 162 `not_started` **111** имеют tool-активность (работа велась, задача формально не завершена), **51** - без активности.

---

## 6. Примечания

- Дублирование диалога в `-plan.md` и `.md` удваивает статистику - при подсчёте итераций учитывать только основные `.md` (не `*-plan*.md`).
- `.tmp` файлы - снапшоты незавершённых задач, не анализируются как отдельные задачи.
- Для завершённых задач tool-метрики берутся из блока `## Completion report` в `.md`; `.tools.log` после завершения удаляется. Для незавершённых задач - из `.tools.log`.
---

## 7. Архивация завершённых задач

- Периодически (раз в месяц) переносить файлы задач со статусом `completed`/`reopened` (есть TaskComplete), завершённых более 1 месяца назад, в подкаталог `.tasklog/archive/`.
- Имя файла архива **всегда** включает диапазон дат завершения задач:
  `.tasklog/archive/archive-YYYY-MM-DD_YYYY-MM-DD.md` (один архив на батч).
- Переносить **все** файлы задач данного периода: `<id>.md`, `<id>-plan*.md`, `<id>.tmp`, `<id>.tools.log`.
- Каталог `.tasklog/` и файлы **не удаляются** - только перемещение в подкаталог `archive/`.
- Статистика сканирует **только корень `.tasklog/`** (архив не учитывается).
