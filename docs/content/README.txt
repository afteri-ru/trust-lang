Document project in Docsy format (symlink to site/content)
1. Base language - russian
2. Manual copying ru/docs files to en/docs dirs
3. Handmade translate *.md and *.html files to target (en) language
4. Convert /ru/ links to default (en) language
5. Copy CNAME to target dir (docs/gh-pages)
6. Copy folder `gh-pages` to `gh-pages` branch into `git@github.com:afteri-ru/trust-lang.git`
