# MEMORY.md

> scope: test
> role: persistent-memory
> last_reviewed: 2026-08-19
> review_period: 30
> max_size: 4096

#  правила для агента

Внеменные файлы всегда должны создаваться в каталоге сборки (`_build`) и оставться  после тестов (не должны удялться).
Все артефакты собираются в TEST_DATA_DIR.

## LIT-тесты: Формат RUN строки

```
# RUN: %trust -q [options] %s > %t.out && %FileCheck %s <%t.out
```

- Вывод всегда во временный файл (`> %t.out`), FileCheck читает из него (`<%t.out`)
- Без `2>/dev/null` — вместо этого `-q`
- Без пайпов — сначала файл, потом чтение
- Без абсолютных путей — только `%FileCheck`, `%trust`
- Данные для Trust — непосредственно в файле, без `echo` или `sed`

TrustLang игнорирует строки с `#`. Поэтому `# CHECK:` метки можно размещать в том же файле.
