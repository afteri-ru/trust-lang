# RUN: %trust -q --emit-ast %s > %t.out && %FileCheck %s <%t.out

# Простое имя
x;
# Локальное имя
$loc;
# Статическое имя (qualified с ::)
::Foo;
# Статическое имя (multi-level)
::Foo::Bar;
# CHECK: ScopeBlock
# CHECK-NEXT:   Ident
# CHECK-NEXT:   Ident
# CHECK-NEXT:   Ident
# CHECK-NEXT:   Ident