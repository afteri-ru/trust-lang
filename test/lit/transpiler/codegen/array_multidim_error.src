# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out

# Многомерный литерал (вложенные массивы-литералы) — генерация не реализована
# (транслируются как тензоры LibTorch в перспективе) → диагностика.

a := [[1,2,],[3,4,],];
# CHECK: error: многомерные массивы пока не реализованы
