# RUN: %trust -q --temp-dir %T --emit-cpp %s > %t.out && %FileCheck %s <%t.out

# Простое объявление переменной с целочисленным литералом (без типа → выведенный минимальный Int)
x := 42;
# CHECK: int8_t c_x = 42;