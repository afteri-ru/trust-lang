# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out

# Вложенный LHS (`d['x'] []= v`) отложен → ошибка компиляции.

@main() := {
    d := (1, two=2,);
    d['x'] []= 7;
}
# CHECK: error: вложенный append '[]=' пока не реализован: append допустим только к простому контейнеру
