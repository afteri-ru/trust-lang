# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out

# Статический индекс вне диапазона массива: `a[5]` при известной размерности 3.

a := [10,20,30,];
b := a[5];
# CHECK: error: array index 5 out of range (size 3)
