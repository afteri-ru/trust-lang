# RUN: %trust -q --temp-dir %T --emit-cpp %s > %t.out && %FileCheck %s <%t.out

# Проверка обобщённого типа Integer → int64_t (несколько переменных)
x:Integer := 42;
# CHECK: int64_t c_x = 42;

y:Integer := 100500;
# CHECK-NEXT: int64_t c_y = 100500;