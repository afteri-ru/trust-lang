# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
# CHECK: error: type sets in a function parameter type are not implemented yet

# Inline-набор в сигнатуре: распознаётся, разворот отложен.
idk(x:(:Int8 + :Int16)):Int64 := { @return :Int64(x); };
@main() := { @return 0; }
