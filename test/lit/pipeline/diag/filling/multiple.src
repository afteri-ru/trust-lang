# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
#
# В одном списке допустим только ОДИН FILLING.
# CHECK: error: элементы массива: многоточие допустимо только один раз
@main() := {
    v:Int32[5] := [1, ... 2 ..., ... 3 ...,];
    @return 0;
}
