# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
#
# FILLING вне списка аргументов вызова/элементов массива (кортеж, словарь) - не самостоятельное
# выражение: точная диагностика (не placeholder).
# CHECK: error: FILLING '... expr ...' требует известного числа позиций (укажите размер массива или сигнатуру вызываемого)
# CHECK: error: FILLING '... expr ...' требует известного числа позиций (укажите размер массива или сигнатуру вызываемого)
@main() := {
    t := (1, ... 2 ...,):Tuple;
    d := (1, ... 2 ...,);
    @return 0;
}
