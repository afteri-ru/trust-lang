# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
#
# Явных элементов массива больше, чем позиций в типе-цели.
# CHECK: error: элементы массива: явных элементов (3) больше, чем позиций (2)
@main() := {
    v:Int32[2] := [1, 2, 3, ... 42 ...,];
    @return 0;
}
