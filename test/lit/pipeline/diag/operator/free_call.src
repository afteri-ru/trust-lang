# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out

# `()`/`[]` - только member-формы (требование C++: operator()/operator[] не бывают свободными).
`()`(a: Int32): Int32 := { return a; };
# CHECK: error: operator '()' must be declared as a class method
