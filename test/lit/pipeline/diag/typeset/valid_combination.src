# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
# CHECK: error: type set definitions are not implemented yet

# Корректная комбинация проходит ПРОВЕРКУ; разворот отложен.
# :Int64 шире :Int32 (новая ветка не уже); :Int32 - подтип ветки :Int64.
x ::= :Int32 + :Int64 - :Int32;
@main() := { @return 0; }
