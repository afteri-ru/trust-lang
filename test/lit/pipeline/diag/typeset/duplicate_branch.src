# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
# CHECK: error: duplicate type set branch start 'Int8'

# Дубль начального типа ветки - ошибка.
x ::= :Int8 + :Int16 + :Int8;
@main() := { @return 0; }
