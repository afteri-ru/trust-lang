# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
# CHECK: error: type 'Float32' is not a subtype of the type set branch start

# '-' допустим только для подтипа начального типа ветки.
x ::= :Int8 + :Int16 - :Float32;
@main() := { @return 0; }
