# RUN: %not %trust -q --emit-cpp %s > %t.out 2>&1 && %FileCheck %s <%t.out
# CHECK: error: type sets in a function return type are not implemented yet

# Inline-набор в возврате: распознаётся, разворот отложен.
f(x:Int64):(:Int8 + :Int16) := { @return x; };
@main() := { @return 0; }
