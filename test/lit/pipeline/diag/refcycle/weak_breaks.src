# RUN: %trust -q --emit-cpp -Wsigil=ignore %s > %t.out 2>&1
# RUN: %FileCheck %s <%t.out

# Weak-ссылка разрывает цикл владения: диагностики НЕТ.
:Node ::= :Class{ next : @[reftype("weak")@] Node := _; };
@main():Int32 := { return 0; };
# CHECK: struct c_Node
