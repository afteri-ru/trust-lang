# RUN: %trust -q --emit-cpp -Wsigil=ignore -Wrecursive-value=ignore %s > %t.out 2>&1
# RUN: %FileCheck %s <%t.out

# Управление severity: -Wrecursive-value=ignore подавляет value-цикл.
:Node ::= :Class{ next : Node := _; };
@main():Int32 := { return 0; };
# CHECK: struct c_Node
