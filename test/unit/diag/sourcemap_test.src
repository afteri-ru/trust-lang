# Исходный файл для юнит естов SourceMap

@@ adder $arg @@ := @@ adder += @$arg @@;
@@ increment(arg) @@ := @adder @$arg 1;

{%
    #include <cstdio>    
%}

i := 0;

increment(i);
increment(i);
increment(i);
increment(i);

hello:String = 'Hello world!';

{%
    printf("%s %d", hello, i);
%}
